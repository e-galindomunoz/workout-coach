import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function WorkoutTabScreen() {
  return (
    <PlaceholderScreen
      title="Workout"
      description="Workout planning and active session flows will be implemented in a later stage."
    />
  );
}
